# What's next — shared footer

At the very end of any completed flow (after confirming the result and giving the Figma link), show this "What's next" block so the user discovers the other tools. Render it exactly as a short list, adjusting only to *omit the command that was just run* so you don't suggest repeating the same flow.

---

**What's next?** You can also run:

- **`/modes-map`** — build a side-by-side mapping table between two variable modes in Figma.
- **`/brand-upload`** — create a "Brand Foundations" variable collection from brand guidelines.
- **`/component-updates`** — document what changed between a library component and a modified version, for developer handoff.

---

Keep it to this list — one line per command, no extra preamble. When a new command is added to the plugin, add one line here and every flow picks it up automatically.
